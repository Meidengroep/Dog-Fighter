using UnityEngine;
using System.Collections;

public static class PropagateLayer 
{
    ///
    /// Use the function IterativeLayerAssignment in TeamHelper.cs
    ///

    //public static void PropagateLayerToChildren(GameObject obj)
    //{
    //    int childCount = obj.transform.childCount;

    //    for (int i = 0; i < childCount; i++)
    //    {
    //        GameObject child = obj.transform.GetChild(i).gameObject;
    //        child.layer = obj.layer;
    //        PropagateLayerToChildren(child);
    //    }
    //}
}
